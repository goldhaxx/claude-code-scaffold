# Plain Rule

This rule file has NO YAML frontmatter — it predates the BTS-385 tier model.

The substrate must return the back-compat default envelope (tier=0, scope=universal, stack=any, anchors={}) without erroring.
