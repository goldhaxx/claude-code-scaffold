# Ideas

- [x] 2026-04-09: Mature the feature lifecycle: branch creation should move to pre-spec (post-idea), and the PR/merge/land tail should be clearly documented as distinct steps. Currently activate creates branches but it happens after spec writing. The full chain should be: idea → branch → spec → plan → implement → complete → merge → land, with each transition point clearly defined. <!-- status:parked -->
- [x] 2026-04-10: Bug: /init in new downstream node projects doesn't register the project with the hub. Need to diagnose and resolve. <!-- status:merged -->
- [x] 2026-04-10: ideas.md timestamps use date format — should use epoch (unix timestamp) for consistency with spec/plan/checkpoint metadata <!-- status:merged -->
- [x] 2026-04-10: Addendum to previous: ideas.md should lead with epoch timestamp but also include a human-readable date/time <!-- status:merged -->
- [x] 2026-04-10: Ideas should have a UID; feedback on a previous idea should update that entry instead of creating a new one <!-- status:promoted -->
- [x] 2026-04-10: Bug: init-apply jq error 'Cannot index object with number' at init-plan.json:78 during new project init (taxes). Likely jq array vs object indexing issue in cmd_init_apply. <!-- status:merged -->
- [x] 2026-04-10: Addendum to init-apply bug: root cause is init-preflight outputs {plan:[...], summary:{...}} but init-apply expects a bare array. Workaround: jq '.plan' to extract. Fix: init-apply should handle the wrapper object, or both commands should agree on format. <!-- status:merged -->
- [x] 2026-04-10: Bug: /init doesn't create a .gitignore for new projects and doesn't register the project with the hub. Init should always create a default .gitignore and run registration. <!-- status:promoted -->
- [x] 2026-04-10: Evaluate whether checkpoint is still needed given /compact, /catchup, and auto-memory. Original purpose: retain context/decisions/insights before clearing. May be redundant now — or may need to evolve into something more targeted (e.g., decision log, session summary for auto-memory). Determine if it should become a /checkpoint skill, merge into another mechanism, or be retired. <!-- status:parked -->
- [x] 2026-04-11: Bug: init-apply fails with jq error 'Cannot index object with number' — init-preflight outputs {plan:[...], summary:{...}} but init-apply expects a bare JSON array. Need to reconcile the format between preflight output and apply input. Reproduce: run init-apply on a new project (e.g. tcg-toolbox). Related to existing ideas #6/#7. <!-- status:promoted -->
- [x] 2026-04-11: Bug: init on new empty project fails when copying git hooks — cp .ccanvil/templates/github/pre-push .git/hooks/pre-push fails because the template file doesn't exist. The init process assumes .ccanvil/templates/github/pre-push is present but it's not in the preset. Either add the template or remove the hook-copy step from init. <!-- status:merged -->
- [x] 5817 1776117229: Replace /clear with /compact throughout ccanvil workflow — update checkpoint flow, workflow rules, and all lifecycle references to prefer /compact over /clear for better context retention across sessions, working in conjunction with checkpoint and /catchup <!-- status:promoted -->
- [x] e4f1 1776271401: BUG: docs-check.sh activate rejects specs with status Draft — should accept Draft or Ready, or the /spec skill should output Ready by default. Currently requires a manual status flip commit before activate works. <!-- status:promoted -->
- [x] b0e2 1776279826: Tech stack distribution: ccanvil currently distributes methodology (rules, commands, hooks) but not tech stack patterns. Need a mechanism to centralize, codify, and distribute tech stack profiles — e.g. FastAPI/SQLite, Next.js/Postgres — so that enhancements made in one project (API-first access rules, protect-db hooks, schema patterns) propagate to all projects sharing that stack. First stack profile: FastAPI/SQLite (proven in taxes project). Includes: CLAUDE.md API-first section, protect-db.sh PreToolUse hook, schema.sql conventions. Design questions: where do stack profiles live (ccanvil/stacks/?), how does /init opt into a stack, how do stack-specific hooks/rules sync alongside hub-universal ones. URGENT USE CASE: fieldnation-toolbox (~projects/fieldnation-toolbox) adopted the FastAPI/SQLite stack from taxes by having Claude review the taxes project directly — but this happened before protect-db.sh and the API-first rules were added, so fieldnation-toolbox has no mutation gating and its agent is already running direct SQL mutations against the DB. This is exactly the drift problem tech stack distribution solves: stack enhancements in one project don't reach projects that borrowed the stack ad-hoc. fieldnation-toolbox needs protect-db.sh + API-first CLAUDE.md rules ASAP. <!-- status:promoted -->
- [x] e376 1776279827: API-first database access paradigm (hub-level): Abstract the API-first data access rule from taxes project into a hub-distributable pattern. Tech-stack-agnostic principles: single writer, API dogfooding, mutation gating, PreToolUse enforcement. Research backing: Bezos API Mandate, Stripe DocDB proxy, Google NoPe suite, SOC 2/HIPAA/PCI-DSS compliance alignment. Concrete deliverables: (1) guide doc or foundations addendum codifying the research, (2) generic protect-db hook template parameterized by DB type, (3) CLAUDE.md section template for API-first rules. First implementation: FastAPI/SQLite (taxes project, already proven). <!-- status:promoted -->
- [x] 8ef0 1776293947: Integrate Nate's Three-Layer Solution for combating dark code into ccanvil: (1) Spec-Driven Development — force comprehension before code generation, spec as requirement + evaluation criteria (ccanvil already does this but may need strengthening), (2) Self-Describing Systems — restructure codebases so every module contains structural manifests and semantic context (rules of engagement, failure modes, behavioral contracts) readable by humans and AI, (3) Comprehension Gate — review step where senior engineers + AI pose critical questions about design and dependencies to ensure code remains legible and accountable. Source: https://www.youtube.com/watch?v=E1idsrv79tI (timestamps: spec-driven 13:00-16:00, self-describing 17:30-20:30, comprehension gate 20:30-23:00). Full transcript review pending. <!-- status:parked -->
- [x] c3c2 1776400296: enhance or change the /pr command to something else that takes into account whether the repo is local only, has no remote/isn't on github because I often run /pr when working in other projects that aren't on gh and may never go to gh, but they need a mature local development workflow, so the command should be something like /merge or something like that which takes the current working branch changes and brings into main, moves back to main as active branch, if its a gh repo, do this as a PR, if its a local repo, checkout main, merge from feature branch, commit to main, or something like that... idk, whatever is best/most mature for working on local repos. <!-- status:promoted -->
- [x] 4aca 1776546594: pull-plan treats files with origin 'stack:<id>' as removed-from-hub because it only looks at the hub root, not hub/stacks/<id>/. Fix: classify stack-origin files separately — either resolve via hub/stacks/<id>/ tree, or skip the removed-from-hub check entirely for non-hub origins. Surfaced when resolving taxes conflicts (protect-db.sh kept flagging as 'removed' every broadcast despite keep-local). <!-- status:promoted -->
- [x] 93e3 1776658240: ccanvil-sync.sh relocate <new-path> subcommand: rename ~/.claude/projects/<old-encoded> to <new-encoded> and rewrite embedded cwd fields in all .jsonl session files. Invoked from the new project location after mv. Preserves Claude Code conversation history across project moves. Medium impact — happens on every migration and users won't remember manually. <!-- status:promoted -->
- [x] 1e15 1776658246: pull-apply take-hub should auto-reinvoke stack-apply for any active stacks on the node. After take-hub on settings.json, stack-specific hook entries are wiped because hub settings doesn't (and shouldn't) include them. Currently Claude manually runs stack-apply <id> to restore. Script should detect active stacks from lockfile and auto-reinvoke. Low-medium impact — only bites on stack-tagged nodes when hub's settings.json changes. <!-- status:promoted -->
- [x] 9289 1776658251: cmd_register should auto-commit the .claude/ccanvil.local.json file it creates. Currently register generates the UUID file as untracked, which blocks the next broadcast pre-check. Requires manual ALLOW_MAIN=1 git add/commit in the node before broadcast can proceed. Mirror the commit_hub_file pattern used on the node side. Medium impact — every first register on a gitignore-respecting node hits this. <!-- status:promoted -->
- [x] 3bad 1776739814: Speed up bats test suite (currently >2min for 516 tests). Look for bats-equivalent of uv-vs-pip speedup. Options to investigate: (1) bats --jobs N for parallel execution, (2) shared setup fixtures across tests (avoid re-creating HUB/NODE dirs per test), (3) replace mktemp/git init loops with a single cached fixture shared read-only across tests, (4) alternative test runners (shellspec, shunit2, bash_unit), (5) profile which tests are slowest (bats --timing) and optimize them individually. <!-- status:promoted -->
- [x] 27b5 1776748802: Spec lifecycle directories: move completed specs to docs/specs/completed/ (or similar), keep active/draft specs in docs/specs/. Reduces noise in list-specs, makes backlog navigation clearer. Consider: does docs-check.sh need path-aware lookups? How does it affect the existing archived-spec convention? <!-- status:merged -->
- [x] 2609 1776791808: Rework checkpoint skill into a dedicated slash command (not stochastic trigger). End-of-session strategic microscope/macroscope that runs BEFORE /compact: reviews session state, project state, active initiatives, backlog, progress; derives cross-session insights into roadmap/radar; runs determinism + security reviews; captures recurring problems (ccanvil bugs, installed-project bugs). Rename to avoid overlap with git checkpoint/rewind — pick a StarCraft-flavored name (e.g., Pylon, Nexus, Overmind, Archive, Beacon, Observer, Chrono, Stasis). Must be the natural companion to /compact. Current skill: .claude/skills/checkpoint (if exists) or template at .ccanvil/templates/checkpoint.md. <!-- status:promoted -->
