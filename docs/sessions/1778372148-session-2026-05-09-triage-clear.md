# Stasis: session-2026-05-09-triage-clear

> Feature: session-2026-05-09-triage-clear
> Kind: session
> Last updated: 1778372148
> Session: 38
> Boundary: 2026-05-09T17:15:48-07:00

## Accomplished

Short post-compact orientation pass on a clean main (post BTS-384 ship). Three substantive operations:

* **Triage queue cleared 3 → 0.** Ran `/idea triage` on BTS-405/406/407 (the 3 mid-session captures session 37's stasis flagged as untriaged):
  * **BTS-407** (FIX: `operations.sh idea.add` resolver emits `--project ''` instead of `--project-id`) — promoted to Backlog @ P2 (High). Genuinely new substrate bug, hit 5x in BTS-400's session.
  * **BTS-406** (Determinism: `/pr` body refresh on existing-not-draft PR) — merged as duplicate of **BTS-397** (already P3 Medium, same anchor + proposal).
  * **BTS-405** (Determinism: `/idea --body-file` flag) — merged as duplicate of **BTS-398** (already P3 Medium, same anchor + proposal).
* **Strategic orientation** via `/recall` + `/radar`. No theme drift action taken — observation only.
* **No code changes.** Tree unchanged from PR #173 squash-merge.

## Current State

* **Branch:** `main` (clean, post-ship — BTS-384 PR #173 merged session 37).
* **Tests:** Tree unchanged since PR #173 — full-suite PASS 2133/0/2133 from prior `/pr` invocation. Re-run skipped per BTS-383/118 single-invocation discipline (no diff to invalidate prior result).
* **Uncommitted changes:** none.
* **Build status:** clean. Manifest 194/194 drift 0, status=ok.

## Blocked On

Nothing.

## Next Steps

1. **BTS-394 + BTS-395 (P2 pair)** — security-audit false-positives. \~30 min each, full downstream-fleet impact. Per radar: cleared-queue → ship cadence test, re-anchors to onboarding-adjacent quality (security-audit fires on fresh-node runs).
2. **BTS-407 (P2)** — newly promoted substrate fix for the `--project ''` empty-arg bug on Linear-routed downstream nodes with `project_id`. Quick fix; affects every `/idea` capture on routed nodes.
3. **Onboarding theme cluster (P2)** — BTS-314 (heal inbox + microsoft365), BTS-324 (routing.ticket→routing.idea rename), BTS-327 (init fresh-mode CLAUDE.md), BTS-337 (provider-heal legacy-data-scan), BTS-312 (test-runner indirection). Re-anchor to active theme after frontmatter+velocity arc detour.
4. **BTS-204 — SSOT-Linear** (Triage; major effort, dedicated session).

## Context Notes

* **Theme drift observation, not action.** Last 5 ships (BTS-385/386/387/384/383) were a frontmatter+velocity substrate arc — soft-aligned with Goal 4 (self-stabilizing system) but NOT in the active **Onboarding & Hub/Spoke Separation** theme. Worth re-anchoring next pick to the theme rather than continuing the substrate detour. /radar flagged this; logged here for the next-session operator to weigh.
* **Context budget at 102.3% (CRITICAL).** BTS-387 landed it from 151% → 102%. Settings.json (1539 tok / 19.2%) and project CLAUDE.md (1009 tok / 12.6%) are now the biggest single contributors. Not blocking, but the ceiling is breached — next budget pass should target settings.json or the CLAUDE.md project-section trim.
* **Triage observation (worth flagging not capturing):** 2 of 3 mid-session captures (BTS-405 → BTS-398, BTS-406 → BTS-397) were within-session duplicates of items captured \~2 hours earlier in the same session. Both pairs share the BTS-393 stasis as origin anchor. The dup-rate signals that mid-session captures don't dedup against same-day captures by default — BTS-115's exact-title dual-capture dedup catches stasis-level dups but not free-text `/idea` captures. Not actionable yet (sample size = 1 session); watch on next high-capture session.
* **Linear-routed stasis flow validated again.** Session 37 used `ALLOW_CONCURRENT_EDIT_OVERRIDE=1` for re-dispatch; this session is a fresh write so override not needed. The pattern (`/tmp/<name>.md` via Write tool → cat | artifact-write) continues to hold.

## Determinism Review

operations_reviewed: 18
candidates_found: 0

No candidates this session. The session was almost entirely deterministic by construction:

* `/recall` and `/radar` are read-only orient skills — all data via existing substrate (`docs-check.sh lifecycle-state`, `radar-gather`, `session-info`, `idea-count`, `permissions-audit.sh`, `module-manifest.sh validate`, `linear-query.sh list-issues`).
* `/idea triage` outcomes dispatched via `operations.sh resolve ticket.transition` + eval — promote with `--priority`, merge with `--duplicate-of`. All three transitions completed in one Linear API call each, no MCP indirection.
* Triage dedup against existing backlog used `mcp__claude_ai_Linear__get_issue` to read full bodies (5 issue reads) — semantic same-fix-different-wording detection is inherently stochastic and not a determinism candidate (no script replaces "is this proposal the same shape as that one?").

Stochastic operations were limited to the radar synthesis prose and the triage rationale — appropriate Claude work, not script-replaceable.

## Evidence Gaps

No evidence gaps this session.

## Manifest Coverage

194 / 194 (allowlist), drift incidents: 0. Info-warn count: 12 (8 rule-tier-budget-exceeded — BTS-386 advisory; 4 rule-vocabulary-leak — BTS-384 advisory, soak-window deferred). Status: ok. Unchanged from session 37.

## Cross-Session Patterns

* **Recurring pattern (positive, holding): zero-candidates determinism review across consecutive sessions.** Sessions 35/36/37/38 all reported `candidates_found: 0`. Four consecutive clean stasis snapshots — substrate maturity signal. The frontmatter atomization arc (BTS-385/386/387/384) plus velocity substrate (BTS-383) shipped without introducing new stochastic ops to flag.
* **Recurring pattern (positive, holding): full-bats-runs-during-iteration discipline.** Session 38 explicitly skipped re-running the suite on an unchanged tree, citing BTS-383/118. Discipline soaking in.
* **No legacy-refs drift** (legacy-refs-scan: empty).
* **No new substrate-collision-mid-PR** — no PR was attempted this session, so the pattern is not exercised.
* **New observation (sample size 1, not a pattern yet): same-session capture duplicates.** 2 of 3 triaged items were dups of same-day captures (BTS-405→398, BTS-406→397). If this recurs on the next high-capture session, lift to a pattern + capture an `idea.find-similar` substrate proposal.

## Security Review

PASS — tree unchanged since PR #173 squash-merge. No diff to scan. Session 37's security-audit baseline (9 pre-existing findings, 0 introduced by BTS-384) remains the canonical state.

## Memory Candidates

No candidates this session. The substantive learning of this session — 2/3 mid-session captures were dups — is logged in Context Notes and Cross-Session Patterns rather than memory, since (a) sample size is 1 and (b) the right next action is to watch for recurrence, not bake it into substrate yet.