# Stasis: session-2026-05-16-bts-507-ship

> Feature: session-2026-05-16-bts-507-ship
> Kind: session
> Last updated: 1778986700
> Session: 58
> Boundary: 2026-05-16T15:05:03-07:00
> Session objective: Take BTS-507 (bats-report-stub-helper) end-to-end — /spec → critic-mode (2 passes) → /activate → /plan → 6-step TDD implementation → /review with WARN fixes → /pr → /ship. Triage 14 captured-from-BTS-497 ideas first, end on main post-ship.

## Accomplished

* **/idea triage of 14 items** captured during the BTS-497 cluster. 11 promoted to Backlog (3× P2 BTS-507/504/498, 8× P3); 3 deferred to Icebox (BTS-500/501/502 — premature without Stage 1 soak baseline). Triage queue cleared 14 → 0.
* **BTS-507 SHIPPED** end-to-end in PR #189 (squash-merged 2026-05-17T02:50:25Z). 7 substantive commits + 1 doc/cleanup commit, then squash-merge to main.
  * Helper substrate at `hub/tests/_helpers/bats-report-stub.bash` — single `stub_bats_report_prewarm()` function writes canonical zero-coverage manifest envelope to `$BATS_FILE_TMPDIR` and exports `BTS_MANIFEST_VALIDATE_CACHE`. Idempotent via overwrite.
  * Drift-guard at `hub/tests/bats-report-stub-drift-guard.bats` — scans `hub/tests/*.bats` (one level, non-recursive) for non-comment-line mentions of `bats-report.sh`; flags any without helper-load OR `# bats-report-stub: exempt`. 5 @tests cover compliant / exempt / non-compliant (path-in-output per AC-5) / non-recursive (AC-6) / production-scan (AC-4).
  * 10 caller files refactored: 6 inline-bypass collapsed to helper-load + 4 silent toll-payers (`bats-report.bats`, `bats-report-jsonl-write-failure`, `bats-report-metrics-envelope`, `bats-report-perf-core-default`) newly bypassed. 2 false-positive files (`rule-vocabulary-leak`, `test-suite-run`) marked exempt.
  * Mid-implementation spec scope-up at Step 4: production scan revealed 11 files vs spec's predicted 6. AC-4 regex broadened from `bash[^\n]*bats-report\.sh` (direct-only) to non-comment-line containing `bats-report.sh` (catches variable-indirect + dispatcher-forwarded). Spec re-dispatched to Linear Document.
  * 5.1 race-fix in `bats-report-metrics-envelope.bats`: 2 inner `--parallel` invocations sped up \~7 min by helper bypass, exposing pre-existing OTel Collector flush race. Added `--no-telemetry` to skip flatten. Envelope `.jobs`/`.parallel` assertions unchanged.
  * /review: 0 BLOCKING, 2 WARN (helper EXIT-trap contract / drift-guard dispatcher gap), 2 INFO. Both WARNs addressed in-band via documentation-only header comments (commit `c806a2c`).
* **3 follow-up tickets active** in Backlog: **BTS-504** (149 remaining bats files, helper rollout), **BTS-505** (test.error_excerpt capture), **BTS-506** (OTel stack-state in /radar). 1 new captured: **BTS-509** (Determinism: exercise-AC-mandated-regexes-against-codebase-pre-impl — see below).

## Current State

* **Branch:** `main` (post-ship, clean)
* **Tests:** 2,432 / 2,432 PASS at commit `b52d20b` (parallel-12, wall 342.7s). State-driven skip on re-run for stasis per `feedback_test_discipline_state_intent_logic.md` — no runtime-bearing files modified since.
* **Uncommitted changes:** none (this stasis pending).
* **Build status:** clean. Manifest 198/198 covered, drift 0 (verified at /review Step 0; no manifest-tracked files modified after).
* **Backlog:** 51 Backlog + 1 Triage (BTS-509). 3× P2 next-up: BTS-507 ✓ shipped (this session), BTS-504 (next BTS-497-cluster ship, bundles 505+506), BTS-498 (drift-guard 5.5-min optimization, independent).
* **OTel stack:** still running. Not actively required for the BTS-507 work; persists between sessions per session 57's convention.

## Blocked On

Nothing.

## Next Steps

1. **BTS-504** is the natural next ship — 149-file helper rollout via deterministic injector. Bundles BTS-505 (error_excerpt) + BTS-506 (stack-state surfacing). Substrate-ready (BTS-507 just landed the helper + drift-guard pattern that BTS-504 leverages at scale).
2. **BTS-498** (drift-guard 5.5-min optimization) is independent — single biggest test-wall savings (\~80% of parallel-12 wall per session 57 data). No dependencies on BTS-507.
3. **BTS-508** (test-run discipline research) — the framework lap. Should land before BTS-504 makes test-gate decisions load-bearing.
4. **BTS-509** (Determinism: exercise-AC-mandated-regexes-against-codebase-pre-impl) — Path 3 doc-rule first (cheapest); Path 1 substrate extension as follow-up if more regex-bearing ACs hit the gap.
5. **Roadmap update.** Active theme is still "Onboarding & Hub/Spoke Separation" (active 2026-05-06), but BTS-497/507 have pivoted toward test-observability as the next emergent theme. Worth an explicit re-anchor before BTS-504 starts.

## Context Notes

* **Substrate scope-up patterns now have 2 validated instances** (BTS-497 → BTS-507). When a grep against the codebase mid-implementation reveals more affected files than the spec listed, scope up + re-dispatch is the right move. Costs \~30 min same-session; partial completion costs more.
* **The test-discipline rule (BTS-497 feedback memory) governed this session well.** 1 full-suite at Step 6 (RED-then-fix-then-GREEN), 1 re-run after the 5.1 race-fix, no reflexive intermediate full-suites. Targeted single-file bats runs at Steps 1/2/3 only. The discipline saved meaningful time.
* **Critic-mode pre-flight + validate-spec are complementary, not redundant.** Critic catches semantic ambiguity (scan-depth confusion between AC-4 and AC-6 — caught in pass 1). Validate-spec is structural (gwt count, file refs). Neither catches regex coverage shortfall against the actual codebase — that's the BTS-509 gap.
* **The 5.1 race-fix is a useful proof point for** `--no-telemetry`**.** BTS-497 Step 14 designed the flag for exactly this case: tests that don't care about telemetry shouldn't pay the flatten cost. The escape-hatch shape is right.
* **Helper-coupling-to-bats-report-trap is a future-fragility note.** `bats-report.sh`'s EXIT trap deletes the stub cache file; the env-var-presence-only check keeps multi-invocation tests safe. If [bats-report.sh](<http://bats-report.sh>) ever extends to READ the cache content, this design needs to track that. Documented in the helper header.

## Determinism Review

* **operations_reviewed:** \~30 (across triage, spec, plan, impl, review, ship)
* **candidates_found:** 1
* **exercise-AC-mandated-regexes-against-codebase-pre-impl**: Claude wrote AC-4 specifying detection regex `bash[^\n]*bats-report\.sh` and ran two critic-mode passes that returned PASS. The critic looked for semantic ambiguity, not regex coverage. The under-specified regex was discovered at Step 4 (production scan flagged 2 files instead of expected 6), forcing a mid-implementation spec scope-up + Linear re-dispatch (\~30 min). Should be: /spec --review critic agent runs the AC-mandated regex against the current codebase + reports match count + 3 example matches; OR a `.claude/rules/tdd.md` addition that requires the spec author to enumerate the expected match set OR provide a one-line grep recipe when an AC contains a regex. Impact: medium — regex-bearing ACs are uncommon, but each occurrence has asymmetric cost (5-min validation vs \~30-min scope-up). Captured as BTS-509.

## Evidence Gaps

* BTS-505 — BTS-497 follow-up: capture test.error_excerpt on failed bats spans — missing-evidence-anchors

(Recurring from BTS-497 session 57 stasis. Operator-owned: either add the 4 anchors to BTS-505 body OR retitle as `DIAGNOSE: error_excerpt never populated on failed bats spans`. Not blocking BTS-504/498/508 work.)

## Manifest Coverage

198 / 198 (allowlist), drift incidents: 0

(Verified at /review Step 0 — `module-manifest.sh validate --json` returned `{coverage:{covered:198,total:198},drift:[],status:"ok"}`. No manifest-tracked files modified after that point — `hub/tests/*` and `hub/tests/_helpers/*` are explicitly out of allowlist per BTS-507 spec Out of Scope.)

## Cross-Session Patterns

* **Substrate scope-up pattern validated 2nd time** (BTS-497 → BTS-507). Documented in `feedback_scope_up_on_live_api_reveal.md` (memory). Pattern: in-flight discovery reveals spec under-counts the universe of affected files; the right move is to update the spec + Linear Document + continue, not to ship narrow-and-defer.
* **Test-discipline rule governed this session well** (BTS-497 → BTS-507). Documented in `feedback_test_discipline_state_intent_logic.md`. Saved \~25-30 min of redundant full-suite runs vs the prior session's pattern.
* **legacy-refs-scan: clean** (`[]`) — no hub-owned or node-specific drift this session.
* **audit-session findings:** 14 total (jq:11, curl:1, shasum:2). All in test files — false positives (jq is a deterministic JSON parser; curl is a healthcheck; shasum is content hashing). No actionable stochastic patterns.

## Security Review

PASS for this session's diff. /review's security-audit surfaced 17 findings (1 CRITICAL, 6 HIGH, 10 MEDIUM) earlier — ALL in files this session does not touch (stasis archives, `hub/meta/operations.md`, prior specs BTS-72/BTS-394/BTS-395). Pre-existing, not introduced.

## Memory Candidates

* No NEW memory candidates this session. The 2 patterns that surfaced (scope-up-on-reveal, test-discipline-state-intent-logic) were both already captured during the BTS-497 cluster.
* BTS-509 idea body is the substrate response to the regex-coverage gap — that's a ticket, not a memory candidate.

<!-- NODE-SPECIFIC-START -->
<!-- Add project-specific content below this line. -->
<!-- Hub content above is updated via /ccanvil-pull. -->